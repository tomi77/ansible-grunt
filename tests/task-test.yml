---
- name: When task is provided
  hosts: all

  roles:
  - role: ansible-grunt
    vars:
      task: build
      chdir: "{{ pwd }}/tests"
      node_version: node
      nvm_init_script: /home/travis/.nvm/nvm.sh

  tasks:
  - name: Assert proper command is executed
    fail:
      msg: bad command executed
    when: grunt_output.results[0].cmd | join(' ') != 'grunt build'
