---
- hosts: localhost
  tasks:
  - name: Install Grunt cli
    npm:
      name: grunt-cli
      state: present
      global: yes
    become: true
    become_user: root

  - name: Install Grunt
    npm:
      name: grunt
      state: present
      path: "{{ pwd }}/tests"
