---
- hosts: all
  tasks:
  # - name: Install required packages
  #   apt:
  #     name: "{{ item }}"
  #   with_items:
  #   - npm
  #   - nodejs
  #   - ca-certificates
  #
  # - name: Bypass https
  #   shell: npm config set strict-ssl false

  - shell: echo ${PATH}

  - shell: whereis yarn

  - name: Install Grunt CLI
    npm:
      name: grunt-cli
      state: present
      global: yes

  - name: Install Grunt
    npm:
      name: grunt
      state: present
      path: "{{ pwd }}/tests"
