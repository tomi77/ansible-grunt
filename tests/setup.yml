---
- hosts: localhost
  tasks:
  - name: Install required packages
    apt:
      name: "{{ item }}"
    with_items:
    - npm
    - ca-certificates
    - node-grunt-cli

  - name: Install Grunt
    npm:
      name: grunt
      state: present
      path: "{{ pwd }}/tests"
