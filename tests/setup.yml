---
- hosts: localhost
  tasks:
  - name: Install NPM
    apt:
      name: npm

  - name: Install Grunt cli
    npm:
      name: grunt-cli
      state: present
      global: yes

  - name: Install Grunt
    npm:
      name: grunt
      state: present
      path: "{{ pwd }}/tests"
